{
  "name": "ticketlemon-gateway",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.1",
    "axios": "^1.13.6",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^3.8.1"
  },
  "module": "src/index.js"
}
